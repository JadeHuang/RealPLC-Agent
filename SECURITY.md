# 安全策略

## 支持范围

| 版本 | 状态 |
| --- | --- |
| 0.4.x | 早期公开测试，接收安全修复 |
| 0.3.x 及更早版本 | 不再作为公开支持版本 |

## 报告安全问题

请勿在公开 Issue 中发布漏洞利用代码、访问令牌、设备密钥、客户数据、工程源码或生产网络信息。

优先使用仓库 Security 页面中的 **Report a vulnerability** 私密报告入口。如果该入口不可用，请仅创建一个标题为 `[Security] Request private contact` 的 Issue，不要填写漏洞细节；RealPLC 将在建立私密沟通渠道后收集完整信息。

报告建议包含：

- 受影响版本和组件；
- 问题类型和影响范围；
- 不包含真实凭据或生产数据的复现条件；
- 建议的缓解措施；
- 是否已在其他位置披露。

## 安全边界

- RealPLC Agent 不能替代工程师审批、现场安全验证或组织的变更管理。
- CODESYS V1 验证链不自动下载到真实 PLC、Control Win 或 SoftMotion Runtime。
- 用户应在测试工程、工程副本、虚拟 PLC 或隔离环境中验证 AI 生成内容。
- 只有来自 RealPLC 官方渠道、版本和 SHA-256 均匹配的安装包才应运行。

## 代码签名状态

RealPLC Agent v0.4.0 安装包尚未使用可信代码签名证书签名。运行前请核对发布页中的 SHA-256。未来稳定版发布流程要求对第一方二进制和安装程序使用可信代码签名证书及 RFC 3161 时间戳。
